import React from 'react';

const Sidebar = () => {
	return (
		<div className='sidebar'>
			<button>Home</button>
			<button>Board</button>
			<button>About Us</button>
		</div>
	);
};

export default Sidebar;
